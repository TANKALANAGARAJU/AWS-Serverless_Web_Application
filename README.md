**AWS-Serverless_Web_Application**    
**Steps**

* Create DynmoDb Table to Store the items.
* Build Lambda Function to Handle the CRUD Operation on DynmoDB Table.
* Use S3 to Store and Host the Web Applications Static Files(HTML,CSS,JS).
* Create a Cloudfrant Distribution to Serve the S3- Hosted static files with low Latency.

